import { ReconnectionConfig, Logger } from '../types';
export declare const DEFAULT_RECONNECTION: ReconnectionConfig;
export declare const DEFAULT_TIMEOUT = 10000;
export declare const DEFAULT_LOGGER: Logger;
